<div class="col-lg-3 col-md-4 sidebar">
    <h4 class="fw-bold mb-4 ps-3">Operator Account</h4>
    <ul class="nav flex-column">
        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["action"].ToString() == "MaintenanceIndex" ? "active text-dark" : "")"
               asp-controller="Operator" asp-action="MaintenanceIndex">
                <i class="fas fa-wrench me-2"></i> Maintenance Requests
            </a>
        </li>

        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["action"].ToString() == "Profile" ? "active text-dark" : "")"
               asp-controller="Operator" asp-action="Profile">
                <i class="fas fa-user me-2"></i> Profile
            </a>
        </li>
    </ul>
</div>